<img src="https://r2cdn.perplexity.ai/pplx-full-logo-primary-dark%402x.png" style="height:64px;margin-right:32px"/>

# この番組、毎週火曜日に放送されるので、できれば毎週更新していきたいです。

更新するための手順を教えて。なるべく丁寧に。

毎週の更新手順をまとめます。大きく「データ取得・要約」と「サイト更新」の2ステップです。

***

## 前提：フォルダの場所

```powershell
cd C:\Users\manab\pikuhami\youtube-radio-archive
```

まずここに移動してから作業します。

***

## ステップ1：新しい放送のトランスクリプトを取得

```powershell
python scripts/fetch_transcripts.py
```

これを実行すると、YouTube の最新動画の字幕データを取得します。
**新しい動画が自動で検出されて追加される**はずです。

***

## ステップ2：AI で要約を生成

```powershell
python scripts/summarize.py
```

または

```powershell
python scripts/batch_summarize.py
```

`scripts/` の中に複数ある場合は、**まだ要約していない動画をまとめて処理するバッチ版**を使うのが楽です。

***

## ステップ3：サイトのデータを更新

```powershell
python scripts/build_site.py
```

これで `src/data/videos.json` が最新の内容に更新されます。

***

## ステップ4：GitHub に push する

```powershell
cd C:\Users\manab\pikuhami
git add .
git commit -m "週次更新 2026-04-XX"
git push
```

push したら Cloudflare Pages が自動でビルド・デプロイするので、数分待てばサイトに反映されます。

***

## まとめると毎週やること

| 順番 | コマンド | 何をするか |
| :-- | :-- | :-- |
| 1 | `python scripts/fetch_transcripts.py` | 字幕を取得 |
| 2 | `python scripts/batch_summarize.py` | AI で要約 |
| 3 | `python scripts/build_site.py` | JSON を更新 |
| 4 | `git add . → commit → push` | サイトに反映 |


***

## 注意点

- **ステップ2は API 料金が発生します**。Claude の Anthropic API を使っているので、毎週数十円〜数百円程度かかる場合があります。
- 放送直後は YouTube の字幕生成が間に合っていないことがあるので、**放送翌日以降に実行する**のが安全です。
- `scripts/` の中のファイル名が実際と違う場合は、`dir scripts` で確認してから実行してください。

***

実際にやってみて、エラーが出たらそのまま貼ってもらえれば対処します。

